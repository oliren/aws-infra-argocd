apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: git-tea
  namespace: argocd
spec:
  project: default
  source:
    chart: gitea
    repoURL: https://dl.gitea.com/charts/
    targetRevision: 12.3.0
    helm:
      releaseName: git-tea
      values: |
        # All overrides, including nested ones, go here
        ingress:
          enabled: true
          className: alb
          annotations:
            alb.ingress.kubernetes.io/scheme: internet-facing
            alb.ingress.kubernetes.io/healthcheck-port: traffic-port
            alb.ingress.kubernetes.io/ip-address-type: ipv4
            alb.ingress.kubernetes.io/listen-ports: '[{"HTTP": 80}, {"HTTPS": 443}]'
            alb.ingress.kubernetes.io/ssl-redirect: "443"
            alb.ingress.kubernetes.io/target-type: ip
          hosts:
            - host: git.aws.lockirin.pp.ua
              paths:
                - path: /
        gitea:
          admin:
            email: gitea@git.aws.lockirin.pp.ua

        global:
          storageClass: gp3

  destination:
    server: "https://kubernetes.default.svc"
    namespace: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true

# project: default
# source:
#   repoURL: https://dl.gitea.com/charts
#   targetRevision: 12.3.0
#   helm:
#     parameters:
#       - name: clusterDomain
#         value: git.aws.lockirin.pp.ua
#       - name: gitea.admin.email
#         value: gitea@git.aws.lockirin.pp.ua
#       - name: ingress.hosts[0].host
#         value: git.aws.lockirin.pp.ua
#       - name: persistence.storageClass
#         value: gp3
#   chart: gitea
# destination:
#   server: https://kubernetes.default.svc
#   namespace: gitea
# syncPolicy:
#   automated: {}
#   syncOptions:
#     - CreateNamespace=true